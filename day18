#include <iostream> 
using namespace std; 
  
void checkyear(int year) 
{ 
    if (year % 4 == 0) 
        cout<<"leap year";
    else    
    cout<<"not a leap year";
} 
  
int main() 
{ 
    int y;
    cin>>y;
    checkyear(y);
}
